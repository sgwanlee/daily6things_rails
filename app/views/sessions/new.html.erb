<%= form_for(:session, url: login_path, html: {class: "login-form center"}) do |f| %>

  <%= f.label :email %>
  <%= f.email_field :email, autocomplete: false%>

  <%= f.label :password %>
  <%= f.password_field :password, autocomplete: false%>

  <%= f.submit "Log in" %>
<% end %>